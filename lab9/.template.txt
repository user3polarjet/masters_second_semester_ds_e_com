зробимо окремим python скриптом:
* строга типізація
* НЕ  треба використовуавти наслідування
* якщо треба класи, то використовуй dataclasses чи namedtuple
* використовуй функції  
* не використовуй from import просто імпортй модуль і звертайся до класів через модуль.класс чи функція.
* from typing import List, Tuple <---- Це вже deprecated. у пайтоні новому можні просто використовуавти list[T], tuple[T]  
* діаграми не показуй, зберігай у папку build у форматі SVG
SCRIPT_PATH = pathlib.Path(os.path.abspath(__file__))
SCRIPT_DIR = SCRIPT_PATH.parent
BUILD_DIR = SCRIPT_DIR / 'build'
* НЕ БІЙСЯ ПИСАТИ БАГАТО КОДУ
* не пиши комкентарі
* НЕ ВИКОРИСТОВУЙ try except
* ЯКЩО РОБИШ ПЕРЕВІРКИ, ТО ПИШИ assert
* Якщо файлу нема, то хай скрипт впаде, не вкладай дані в код
* коли пишеш звіт не роби уточнення в дужках (), пиши відразу потрібну інформацію
* не пиши у висновку про стандарти коду 
* НЕ БІЙСЯ ПИСАТИ БАГАТО КОДУ І ПОВНІСТЮ ВИКОНУВАТИ ЗАВДАННЯ 

* Виконуй завдання першого рівня складності з додатковим завданням

