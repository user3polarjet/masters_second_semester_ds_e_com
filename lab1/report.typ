#set text(font: "Times New Roman", size: 12pt)
#set page(
  paper: "a4",
  margin: (top: 2cm, bottom: 2cm, left: 2.5cm, right: 2cm)
)
#align(center)[

*Міністерство освіти і науки України*

*Національний технічний університет України “Київський політехнічний інститут імені Ігоря Сікорського”*

*Кафедра обчислювальної техніки ФІОТ*
]

#align(horizon)[
  
  #align(center)[
    *ЗВІТ*

    *з лабораторної роботи №1*

    *з навчальної дисципліни «Технології Data Science для завдань електронної комерції»*
  ]

  #align(left)[
    *Тема:*
  ]

  #align(center)[
    *"ПІДГОТОВКА ТА АНАЛІЗ ДАНИХ ДЛЯ СТАТИСТИЧНОГО НАВЧАННЯ"*
  ]
]

#align(right)[

  *Виконав:*

  Студент 1 курсу кафедри ІПІ ФІОТ

  Навчальної групи ІП-51мн

  Сергій ПАНЧЕНКО

  *Перевірив:*

  Професор кафедри ОТ ФІОТ

  Олексій ПИСАРЧУК
]

#align(center + bottom)[
  Київ 2026
]

#pagebreak()

// #set page(numbering: "1")

#set heading(numbering: (..nums) => nums.pos().map(str).join("."))
#show heading: it => {
  set text(weight: "bold", size: 12pt)
  set align(left)
  if it.level == 1 {
    counter(figure.where(kind: image)).update(0)
    it
  } else {
    it
  }
}
#show figure: it => {
  set align(center)
  it.body
  v(8pt, weak: true)
  it.supplement 
  [ ]
  context (it.counter.display(it.numbering))
  [ — ] 
  it.caption.body
}
#set figure(
  supplement: [Рисунок],
  numbering: (num) => {
    context {
      let h_num = counter(heading).at(here()).at(0)
      str(h_num) + "." + str(num)
    }
  }
)

#show ref: it => {
  let el = it.element
  if el != none and el.func() == figure {
    context {
      let num = el.counter.at(el.location())
      numbering(el.numbering, ..num)
    }
  } else {
    it
  }
}
#set par(leading: 0.65em, spacing: 0.65em, first-line-indent: (amount: 1.25cm, all: true), justify: true)
#set list(indent: 1.25cm)
#set enum(indent: 1.25cm)

= Мета
Виявити дослідити та узагальнити особливості застосування методів статистичного навчання  для  задач  визначення  статистичних  характеристик  вхідного  потоку  даних  з 

= Завдання

Лабораторія  провідної  ІТ-компанії  реалізує  масштабний  проект  розробки
універсальної  платформи  з  обробки  Big  Data  масиву  статистичних  даних
поточного спостереження для виявлення закономірностей і прогнозування розвитку
контрольованого процесу. Платформа передбачає розташування back-end компоненти
на власному хмарному сервері  з  наданням  повноважень  користувачам
заздалегідь  адаптованого  front-end функціоналу універсальної платформи.

Замовниками  ресурсів  платформи  є:  державні  та  комерційні  компанії
валютного трейдінгу  для  прогнозування  динаміки  зміни  курсу  валют  та  ціни
інших  товарів; метеорологічні  служби  для  прогнозування  параметрів
метеоумов;  департаменти  охорони здоров’я для прогнозування зміни показників
епідеміологічних ситуацій тощо.

Вам, як Data Science Engineer поставлено
завдання. 

*Завдання ІІІ рівня – максимально 9 балів.*

+ Провести парсинг самостійно обраного сайту. Вміст даних, що підлягають парсингу – обрати самостійно.  
+ Результати парсингу зберегти у файлі. Тип файлу обрати самостійно. 
+ Оцінити динаміку тренду реальних даних. 
+ Здійснити визначення статистичних характеристик результатів парсингу.
+  Синтезувати  та  верифікувати  модель  даних,  аналогічних  за  трендом  і статистичними характеристиками реальним даним, які є результатом парсингу. 
+ Провести аналіз отриманих результатів.

= Результати виконання лабораторної роботи.

== Синтезована математична модель перетворень графічних об’єктів відповідно до індивідуального завдання.  

== Блок схема алгоритму та її опис. 

== Опис структури проекту програми.

== Результати роботи програми відповідно до завдання.

== Програмний код.

Програмний  код  послідовно  реалізує  алгоритм  рис.1  та  спрямовано  на  отримання  результатів, поданих вище. 

При цьому використано можливості Python бібліотек: pip; pandas; numpy; xlrd; 
matplotlib. 

Контексні  коментарі  пояснюють  сутність  окремих  скриптів  наведеного  коду програми.

#let embed_python(file_path) = {
  heading(file_path, level: 2)
  raw(read(file_path), lang: "python", block: true)
}
// #embed_cpp("count_words.cpp")
// #embed_cpp("gen_dir.cpp")
// #embed_cpp("matrix_transpose.cpp")
// #embed_cpp("pi_monte.cpp")
// #embed_cpp("prime_slice.cpp")

== Аналіз  результатів  відлагодження  та  верифікації  результатів  роботи програми.

= Висновки

У ході лабораторної роботи досліджено ефективність багатопоточної обробки.

CPU-bound: Демонструють майже лінійне прискорення до досягнення ліміту фізичних ядер. Основний фактор успіху — рівномірне балансування навантаження між потоками.

Memory-bound: Швидкодія жорстко обмежується пропускною здатністю оперативної пам'яті. Реального прискорення вдалося досягти лише завдяки оптимізації доступу до кешу (блокове транспонування).

IO-bound: Багатопоточність дає приріст лише на малих кількостях потоків. Далі продуктивність швидко досягає плато через обмеження дискової підсистеми (IOPS), і додавання нових потоків втрачає сенс.
